server:
  port: 9082

spring:
  application:
    name: mcp-client-webflux
  ai:
    openai:
      api-key: sk-4a8abddd743346fa91f1408e7f73daad
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      chat:
        options:
          model: qwen-max
    alibaba:
      mcp:
        nacos:
          enabled: true
          server-addr: http://127.0.0.1:8848
          username: coderpwh_admin
          password: coderpwh_admin
          namespace: f6027be6-8b32-494d-bb55-1112388383a9
          client:
            sse:
              connections:
                server1: mcp-server-provider
    mcp:
      client:
        enabled: true
        name: mcp-client-webflux
        version: 0.0.1
        initialized: true
        request-timeout: 600s

        nacos-enabled: true

        type: sync
        toolcallback:
          enabled: true
        root-change-notification: true


# 调试日志
logging:
  level:
    io:
      modelcontextprotocol:
        client: DEBUG
        spec: DEBUG