server:
  port: 18080
spring:
  application:
    name: big-tool
  ai:
    alibaba:
      toolcalling:
        weather:
          enabled: true
          api-key:
    dashscope:
      api-key:
    openai:
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      api-key:
      chat:
        options:
          model: qwen-max-latest
      embedding:
        options:
          model: text-embedding-v1