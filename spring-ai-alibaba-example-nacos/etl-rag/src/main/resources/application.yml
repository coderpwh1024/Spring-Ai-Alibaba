server:
  port: 8002

spring:
  application:
    name: rag-etl-pipeline

  ai:
    dashscope:
      api-key:
      base-url: https://dashscope.aliyuncs.com/
      chat:
        options:
          model: qwen-max
      embedding:
        options:
          model: text-embedding-v1